using System;

class Program
{
    static void Main(string[] args)
    {

        // Uncomment the scenario you want to run:

      //  BrowserHistory bh=new BrowserHistory("leetcode.com");
      //  bh.Visit("google.com");

      //Console.WriteLine(  bh.Back(1));
      //  Console.WriteLine(bh.Forward(1));
      // Console.WriteLine( bh.Forward(1));
      //  bh.Visit("facebook.com");
      //  bh.Visit("youtube.com");
      // Console.WriteLine( bh.Back(1));

        //MusicPlaylistDemo.Run();
         //UndoFeatureDemo.Run();
         //CustomerServiceDemo.Run();
         //TrainRouteDemo.Run();
         //TaskSchedulerDemo.Run();
         //NotificationFeedDemo.Run();
         //LRUCacheDemo.Run();
         CommandPatternDemo.Run();
        // NotificationSystemDemo.Run();
        // OrderProcessingDemo.Run();
        // JobExecutionDemo.Run();
        // ChatQueueDemo.Run();
        // NotificationRetryDemo.Run();
        // WorkflowEngineDemo.Run();

        Console.ReadKey();
    }
}
